<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.wujunyang.mapper.EmpMapper">

<!--    根据ID更新员工信息-->
    <update id="updateById">
        UPDATE emp
        <set>
            <if test="username != null and username != ''">username = #{username},</if>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="job != null">job = #{job},</if>
            <if test="salary != null">salary = #{salary},</if>
            <if test="image != null and image != ''">image = #{image},</if>
            <if test="entryDate != null">entry_date = #{entryDate},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="updateTime != null">update_time = #{updateTime}</if>
        </set>
        WHERE id = #{id}

    </update>

    <delete id="delete">
        delete from emp where id in 
        <foreach collection="ids" item="id" index="index" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <resultMap id="resultmap" type="org.wujunyang.pojo.Emp">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="gender" column="gender"/>
        <result property="phone" column="phone"/>
        <result property="job" column="job"/>
        <result property="salary" column="salary"/>
        <result property="image" column="image"/>
        <result property="entryDate" column="entry_date"/>
        <result property="deptId" column="dept_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="deptName" column="dept_name"/>
        <collection property="exprList" ofType="org.wujunyang.pojo.EmpExpr">
            <id property="id" column="ee_id"/>
            <result property="company" column="ee_company"/>
            <result property="job" column="ee_job"/>
            <result property="begin" column="ee_begin"/>
            <result property="end" column="ee_end"/>
            <result property="empId" column="ee_empid"/>
        </collection>
    </resultMap>
    <select id="get" resultMap="resultmap">
        SELECT
            e.*,
            ee.id AS ee_id,
            ee.company AS ee_company,
            ee.job AS ee_job,
            ee.begin AS ee_begin,
            ee.end AS ee_end,
            ee.emp_id AS ee_empid
        FROM emp e
                 LEFT JOIN emp_expr ee ON e.id = ee.emp_id
        WHERE e.id = #{id}



    </select>
    <select id="countByGender" resultType="java.util.Map">
        SELECT
            case  when gender=1 then '男'
                  when gender=2 then '女'
                  else '未知' end as 性别,
            count(*) as 人数

        from emp group by gender


    </select>
    <select id="selectByUsernameAndPassword" resultType="org.wujunyang.pojo.Emp">
       select id ,username,name from emp where username=#{username} and password=#{password}
    </select>

    <!-- 查询员工列表 -->
    <select id="list" resultMap="resultmap">
        SELECT 
            e.*,
            d.name as dept_name,
            ee.id AS ee_id,
            ee.company AS ee_company,
            ee.job AS ee_job,
            ee.begin AS ee_begin,
            ee.end AS ee_end,
            ee.emp_id AS ee_empid
        FROM emp e
        LEFT JOIN dept d ON e.dept_id = d.id
        LEFT JOIN emp_expr ee ON e.id = ee.emp_id
        <where>
            <if test="name != null and name != ''">
                AND e.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="gender != null">
                AND e.gender = #{gender}
            </if>
            <if test="begin != null and begin != ''">
                AND e.entry_date >= #{begin}
            </if>
            <if test="end != null and end != ''">
                AND e.entry_date &lt;= #{end}
            </if>
        </where>
        ORDER BY e.update_time DESC
    </select>
</mapper>